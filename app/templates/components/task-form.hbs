<form {{action "save" on="submit"}}>
  <div class="form-group {{if isError 'has-error'}}">
    {{input value=title type="text" class="form-control" id="title" placeholder="Task Item"}}
  </div>
  <button type="submit" class="btn btn-primary btn-block">Create</button>
</form>
